<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lawtest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.LogInActivity">
        </activity>
        <activity android:name=".ui.new_user.NewUserActivity"/>
        <activity android:name=".ui.base.CropActivity"/>
        <activity android:name=".ui.user.UserActivity"/>
        <activity android:name=".ui.admin.AdminActivity"/>
        <activity android:name=".ui.admin.new_specialist.NewSpecialistActivity"/>
        <activity android:name=".ui.specialist.SpecialistActivity"/>
        <activity android:name=".ui.admin.new_service.NewServiceActivity"/>
        <activity android:name=".ui.admin.services.ShowServiceActivity"/>
    </application>

</manifest>